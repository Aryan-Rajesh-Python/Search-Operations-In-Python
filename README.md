in this program we are given two options to search. they are linear and binary. choice is asken and if choose linear then linear function returns the output and if you choose binary function viceversa.
